import sys
import count

n = int(input())


while (n > 0):
    A, B = input("Informe A e B, separado por um espaço: ").split();
    break
    
if count(A) >= len(b):

        if (a[(len(a) - len(b)):]) == b:

            print("encaixa")

        else:

            print("nao encaixa")



