#input
    #é uma funçào builtin (pois já é da linguagem do python)
    #ela recebe um argumento do tipo string, que é exibido para o usuário na saída padr"ao (tela)
    #a função lê a entrada, converte para string e retorna o valor
    
    
    
#print
    #é uma funçào builtin (pois já é da linguagem do python)
    #ela recebe um arguento obrigatório do tipo varargs de objetos e 4 argumentos opcionais (sep, end, file e flush)
    #todos os objetos são convertidos para string, separados por sep e terminados por end
    #a string final é exibida para o usuário